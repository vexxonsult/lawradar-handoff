name: Moteur LawRadar — livraison structurée

on:
  workflow_dispatch:
  schedule:
    # Une file peut être rattrapée par lots après la collecte quotidienne.
    - cron: "35 * * * *"

permissions:
  contents: write
  id-token: write

concurrency:
  group: moteur-lawradar
  cancel-in-progress: false

jobs:
  gate:
    runs-on: ubuntu-latest
    outputs:
      should_run: ${{ steps.delta.outputs.should_run }}
      skip_reason: ${{ steps.delta.outputs.skip_reason }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - id: delta
        name: Préparer le diff et éviter tout appel inutile au modèle
        run: |
          mkdir -p out
          python scripts/manage_motor_queue.py stage --queue evidence/motor-queue-latest.json --queue-output out/motor-queue-proposed.json --batch-output out/motor-input-gate.json --batch-size 10
          python - <<'PY'
          import json
          import os
          from pathlib import Path
          prepared = json.loads(Path("out/motor-input-gate.json").read_text(encoding="utf-8"))
          required = bool(prepared.get("candidates"))
          changed = set(prepared.get("delta_changed_sources", []))
          handled = set(prepared.get("handled_source_files", []))
          unsupported = sorted(changed - handled)
          if required:
              reason = "CANDIDATES_READY"
          elif prepared.get("excluded_historical_candidates"):
              reason = f"{len(prepared['excluded_historical_candidates'])} réapparition(s) historique(s) hors fenêtre quotidienne, non envoyée(s) au moteur."
          elif prepared.get("excluded_routine_candidates"):
              reason = f"{len(prepared['excluded_routine_candidates'])} publication(s) de routine administrative, classée(s) sans appel au moteur."
          elif unsupported == ["eurlex-oj-latest.json"]:
              reason = "Variation EUR-Lex détectée ; hors périmètre du moteur actuel (JORF et ConsultDD)."
          elif unsupported:
              reason = "Variation détectée sur une source hors périmètre du moteur actuel."
          else:
              reason = "Aucun candidat JORF ou ConsultDD à interpréter."
          Path(os.environ["GITHUB_OUTPUT"]).open("a", encoding="utf-8").write(
              f"should_run={'true' if required else 'false'}\nskip_reason={reason}\n"
          )
          print(f"model_input_required={required}; candidates={len(prepared.get('candidates', []))}")
          PY
      - name: Conserver la file avant toute interprétation
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          cp out/motor-queue-proposed.json evidence/motor-queue-latest.json
          git config user.name "lawradar-motor[bot]"
          git config user.email "lawradar-motor[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git add evidence/motor-queue-latest.json
          git diff --cached --quiet || git commit -m "File moteur LawRadar $(date -u +%F)"
          git pull --rebase origin main
          git push

  record_skipped:
    needs: gate
    if: needs.gate.outputs.should_run != 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Démarrer la mesure du run
        run: echo "LAWRADAR_STARTED_AT=$(date +%s)" >> "$GITHUB_ENV"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Enregistrer l'absence d'appel au modèle
        run: |
          mkdir -p out
          python scripts/build_run_manifest.py --output evidence/run-manifest-motor-latest.json --kind motor --status skipped --reason "${{ needs.gate.outputs.skip_reason }}" --inputs evidence/delta-latest.json evidence/jorf-summaries-latest.json evidence/consultdd-latest.json
          python scripts/build_run_index.py --output evidence/run-index-latest.json --manifests evidence/run-manifest-latest.json evidence/run-manifest-motor-latest.json
          python scripts/render_control_dashboard.py --input evidence/run-index-latest.json --out out/control-dashboard.html
      - name: Téléverser le contrôle du run ignoré
        uses: actions/upload-artifact@v4
        with:
          name: lawradar-motor-${{ github.run_id }}
          path: |
            evidence/run-manifest-motor-latest.json
            evidence/run-index-latest.json
            out/control-dashboard.html
          retention-days: 90
      - name: Versionner le statut ignoré du moteur
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "lawradar-motor[bot]"
          git config user.email "lawradar-motor[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git add evidence/run-manifest-motor-latest.json evidence/run-index-latest.json
          git diff --cached --quiet || git commit -m "Moteur LawRadar ignoré $(date -u +%F)"
          git pull --rebase origin main
          git push

  moteur:
    needs: gate
    if: needs.gate.outputs.should_run == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Démarrer la mesure du run
        run: echo "LAWRADAR_STARTED_AT=$(date +%s)" >> "$GITHUB_ENV"
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Vérifier les règles de rendu
        run: python -m unittest discover -s tests -p 'test_*.py'
      - name: Préparer la livraison du run
        run: |
          mkdir -p out
          python scripts/manage_motor_queue.py stage --queue evidence/motor-queue-latest.json --queue-output out/motor-queue-proposed.json --batch-output out/motor-input.json --batch-size 10
      - name: Produire la livraison structurée
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          prompt: >-
            Tu exécutes le moteur LawRadar. Lis uniquement out/motor-input.json et
            config/moteur-delivery-schema.json. N'utilise ni réseau, ni recherche,
            ni Google Drive, ni autres fichiers. Écris exactement out/motor-delivery.json, conforme
            au schéma. N'écris aucun autre fichier. Chaque candidate doit avoir une
            opportunity avec son source_id ; toute information non démontrée est
            UNRESOLVED. Si aucun flux n'est étayé, écris money_flows: [].
            evidence/universal-signal-latest.json est une sortie historique : ne le lis pas,
            ne l'utilise ni comme exemple, ni comme source, ni comme solution de repli.
            Chaque fait, acteur, montant et flux de cette livraison doit venir uniquement
            d'un candidat présent dans out/motor-input.json.
          claude_args: >-
            --model claude-sonnet-5
            --max-turns 6
            --allowedTools Read,Write
      - name: Valider et rendre le dashboard
        run: python scripts/render_motor_delivery.py --input out/motor-delivery.json --dashboard out/dashboard.html
      - name: Publier la livraison du run
        env:
          LAWRADAR_MODEL: claude-sonnet-5
        run: >-
          python scripts/build_run_manifest.py
          --output out/run-manifest.json
          --kind motor --status success
          --inputs out/motor-input.json config/moteur-delivery-schema.json
          --outputs out/motor-delivery.json out/dashboard.html
      - name: Construire le dossier universel de signal
        run: |
          python scripts/build_universal_signal.py --motor-input out/motor-input.json --delivery out/motor-delivery.json --run-manifest out/run-manifest.json --output out/universal-signal.json
          cp out/universal-signal.json evidence/universal-signal-latest.json
          python scripts/build_run_manifest.py --output out/run-manifest.json --kind motor --status success --inputs out/motor-input.json config/moteur-delivery-schema.json --outputs out/motor-delivery.json out/dashboard.html out/universal-signal.json
      - name: Actualiser le registre du centre de contrôle
        run: |
          python scripts/manage_motor_queue.py advance --queue evidence/motor-queue-latest.json --motor-input out/motor-input.json --output evidence/motor-queue-latest.json
          cp out/run-manifest.json evidence/run-manifest-motor-latest.json
          python scripts/build_run_index.py --output evidence/run-index-latest.json --manifests evidence/run-manifest-latest.json evidence/run-manifest-motor-latest.json
          python scripts/render_control_dashboard.py --input evidence/run-index-latest.json --out out/control-dashboard.html
      - name: Téléverser la livraison du run
        uses: actions/upload-artifact@v4
        with:
          name: lawradar-moteur-${{ github.run_id }}
          path: out/
          retention-days: 90
      - name: Versionner le statut du moteur
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "lawradar-motor[bot]"
          git config user.email "lawradar-motor[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          git add evidence/motor-queue-latest.json evidence/run-manifest-motor-latest.json evidence/run-index-latest.json evidence/universal-signal-latest.json
          git diff --cached --quiet || git commit -m "Statut moteur LawRadar $(date -u +%F)"
          git pull --rebase origin main
          git push
